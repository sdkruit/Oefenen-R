# Oefenen-R

Dit is een oefenrepository voor het werken met R en GitHub.  
In dit project wordt het gemiddelde berekend van de variabele `hour` uit het *Boston Crime*-bestand (`crime` dataset uit het R-pakket `ggmap`).

---

## 📂 Bestanden in deze repository

- **gemiddelde_hour.R**  
  Script dat:
  1. Het pakket `ggmap` installeert en laadt (indien nodig)  
  2. De `crime` dataset laadt  
  3. Het gemiddelde van de variabele `hour` berekent  
  4. Het resultaat in de console toont  

---

## ▶️ Gebruik

### 1. Repository clonen
Gebruik onderstaande code in de terminal of RStudio:

```bash
git clone https://github.com/jouwgebruikersnaam/Oefenen-R.git
